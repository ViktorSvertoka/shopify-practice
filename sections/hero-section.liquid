{% schema %}
{
  "name": "Hero Steve",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Заголовок",
      "default": "Steve Jobs"
    },
    {
      "type": "textarea",
      "id": "text",
      "label": "Цитата",
      "default": "Have the courage to follow your heart and intuition. They somehow already know what you truly want to become. Everything else is secondary. — Steve Jobs"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Текст кнопки",
      "default": "Join to Fanclub"
    },
    {
      "type": "url",
      "id": "button_url",
      "label": "Посилання кнопки",
      "default": "/"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Фото"
    }
  ]
}
{% endschema %}

<section class="hero-section">
  <div class="hero-container">
    
    <div class="hero-text">
      <h1>{{ section.settings.title }}</h1>
      <p class="quote">{{ section.settings.text }}</p>
      
      <div class="buttons">
        <a href="{{ section.settings.button_url }}" class="btn primary">
          {{ section.settings.button_label }}
        </a>
        <a href="#" class="btn secondary">Sign In</a>
      </div>

      <div class="socials">
        <a href="#"><span>📸</span></a>
        <a href="#"><span>📘</span></a>
        <a href="#"><span>▶️</span></a>
        <a href="#"><span>🐦</span></a>
      </div>
    </div>

    <div class="hero-image">
      {% if section.settings.image != blank %}
        <img src="{{ section.settings.image | image_url }}" alt="{{ section.settings.title }}">
      {% endif %}
    </div>

  </div>
</section>

<style>
.hero-section {
  background: #fff;
}

.hero-container {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 40px;
  max-width: 1200px;
  margin: 0 auto;
  position: relative;
}

.hero-text {
  flex: 1;
}

.hero-text h1 {
  font-size: 100px;
  font-weight: 900;
  margin-bottom: 20px;
  position: absolute;
  top: 15%;   
  left: 22%;   
  transform: translate(-50%, -50%); 
}

.hero-text .quote {
  font-size: 24px;
  font-style: italic;
  color: #555;
  margin-bottom: 30px;
  position: absolute;
  top: 38%;   
  left: 26%;   
  transform: translate(-50%, -50%); 
  width: 630px;
  height: 132px; 
}

.buttons {
  display: flex;
  gap: 20px;
  margin-top: 80px;
  margin-bottom: 30px;
}

.btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 252px;
  height: 70px; 
  border-radius: 50px;
  font-weight: bold;
  font-size: 18px;
  text-decoration: none;
  transition: all 0.3s;
  text-transform: uppercase;
}

.btn.primary {
  background: #000;
  color: #fff;
}

.btn.primary:hover {
  background: #fff;
  color: #000;
  border: 2px solid #000;
}

.btn.secondary {
  border: 2px solid #000;
  color: #000;
}

.btn.secondary:hover {
  background: #000;
  color: #fff;
}

.hero-image img {
  width: 750px;
  display: block;
}
</style>
